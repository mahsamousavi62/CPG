من به عنوان برنامه کاربردی میخواهم تا در صورت تایید یک درخواست پرداخت، اگر متناظر این درخواست پرداخت یک تراکنش از نوع IPG با سرویس دهنده آسان پرداخت وجود داشت، سیستم نسبت به فراخوانی سرویس تسویه تراکنش اقدام کند


نیاز است تا پس از فراخوانی سرویس Verify (از سرویس های آپ) در صورتیکه عملیات تایید موفقیت آمیز صورت گرفته باشد، اگر نوع سرویس دهنده مربوط به درگاه مربوط به درخواست پرداخت : "آسان پرداخت" باشد، در گام بعدی نسبت به فراخوانی سرویس تسویه تراکنش اقدام شود :
سرویس مذکور با عنوان "Verify" (در دسته بندی ای با عنوان : "IPG") با آدرس و مشخصات ذیل فراخوانی خواهد شد :
URL : https://ipgrest.asanpardakht.ir/v1/Settlement
متد : Post
Headers

ردیف

عنوان

مقدار

توضیحات

1

Accept

application/json

-

2

Content-Type

application/json

-

3

usr

Company_IPG.provider_data

با استفاده از payment_code به Payment_Request و سپس با استفاده از شناسه آن به Company_IPG_ID میرسیم. پس از آن به موجودیت Company_IPG و فیلد Proivder_data مراجعه نمایید. در ستون مذکور مقداری با شناسه User_Name واکشی شود.


4

pwd

Company_IPG.provider_data

با استفاده از payment_code به Payment_Request و سپس با استفاده از شناسه آن به Company_IPG_ID میرسیم. پس از آن به موجودیت Company_IPG و فیلد Proivder_data مراجعه نمایید. در ستون مذکور مقداری با شناسه Password واکشی شود.













Body

ردیف

عنوان

معادل در سرویس

الزامی/اختیاری

نوع

معادل در دیتابیس

مقداردهی

1

کد پیکربندی پذیرنده

merchantConfigurationId

الزامی

numeric

Company_IPG.provider_data

با استفاده از payment_code ارائه شده در موجودیت IPG_Transaction به Company_IPG_ID رسیده و پس از آن به موجودیت Company_IPG و فیلد Proivder_data مراجعه نمایید. در ستون مذکور مقداری با شناسه Merchant_Configuration_Id واکشی شود.

2

توکن

payGateTranId

الزامی

numeric

IPG_Transaction.provider_tracker_id

با استفاده از payment_code ارائه شده در موجودیت IPG_Transaction رسیده و مقدار provider_tracker_id واکشی می شود








نمونه ورودی : 
{

  "merchantConfigurationId": 10000, 

  "payGateTranId": 1901242499

}


1 - برای اولین بار فقط Status_Code برابر با 200 برمیگرداند.
2 - در دفعات بعدی مقادیر ذیل با Status_Code برابر با 474 و مقادیر ذیل بازگردانده می شود : 
{
    "error": {
        "code": 1026,
        "message": "Transaction Is Pending for Reconcilation.",
        "args": {}
    }
}
3 - در صورتیکه قبل از فراخوانی سرویس تسویه سرویس تایید فراخوانی نشده باشد، مقادیر ذیل با Status_Code برابر با 472 بازگردانده می شود : 
{
    "error": {
        "code": 1029,
        "message": "Unverified Transaction.",
        "args": {}
    }
}



 متد  settle بنویس AsanPardakhtProvider این سرویس رو در پروایدر 

 بعدش برو بهVerifyTransactionQueryHandler اینو براش بنویس
 نیاز است تا پس از فراخوانی سرویس Settlement (آسان پرداخت) در صورتیکه هیچ خطا یا مشکلی بروز نکند، متناسب با خروجی سرویس مذکور نسبت به بروز رسانی موجودیت های IPG_Transaction و Transaction اقدام شود
1 - پارامترهای موجودیت IPG_Transaction به شکل ذیل بروز رسانی خواهد شد :
ردیف

عنوان

معادل دیتابیسی

معادل در سرویس

نحوه مقداردهی

1

وضعیت تراکنش درگاه

status

serviceStatusCode

متناسب با مقدار بازگشتی وضعیت اجرای سرویس (status) مقداردهی می شود :

1 - برابر با "200"، "474"، "476" باشد، مقدار این پارامتر برابر با : "9" (ثبت درخواست تسویه موفق)

2 - برابر با "471"، "472"، "473"، "475"، "478" باشد یا اینکه بعد از چندین بار تلاش همچنان عملیات ثبت درخواست تسویه تراکنش با حالات ذکر شده در آیتم 18663 مواجه شود مقدار این پارامتر برابر با : "10" (ثبت درخواست تسویه ناموفق)

2

تاریخ و زمان بروز رسانی

modification_date_time

-

تاریخ و زمان جاری







2 - پارامترهای موجودیت Transaction به شکل ذیل بروز رسانی می شوند :
ردیف

عنوان

معادل دیتابیسی

نحوه مقداردهی

1

تاریخ و زمان احتمالی تسویه

predicted_settlement_date_time


متناسب با مقدار تنظیم شده در موجودیت و فیلد IPG_Transaction.status، مقداردهی می شود

1 - برابر با "9" باشد، مقدار این پارامتر به روش ذیل مقداردهی خواهد بود

1 - 1 - در صورتیکه زمان جاری، کوچکتر از 23:45 باشد. تاریخ = {تاریخ جاری} + 1 روز | زمان = 07:00

2 - 1 - در صورتیکه زمان جاری، بزرگتر یا مساوی 23:45 باشد. تاریخ = {تاریخ جاری} + 2 روز | زمان = 07:00

2 - برابر با "10" باشد، مقدار این پارامتر به روش ذیل مقداردهی خواهد بود

1 - 2 - در صورتیکه زمان جاری، کوچکتر از 20:40 باشد. تاریخ = {تاریخ جاری} + 1 روز | زمان = 07:00

2 - 2 - در صورتیکه زمان جاری، بزرگتر یا مساوی 20:40 باشد. تاریخ = {تاریخ جاری} + 2 روز | زمان = 07:00


2

تاریخ و زمان بروز رسانی

modification_date_time

تاریخ و زمان جاری

